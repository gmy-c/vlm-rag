# Verified project dependencies — sensitivity classification + retrieval/RAG
#
# Verified locally with Python 3.11, RTX 4080 Laptop and CUDA 12.8.
# The same cu128 PyTorch build supports RTX 5090 when the driver is compatible.

# ── PyTorch (CUDA 12.8) ──
# 已验证安装命令（不要写在 requirements.txt 中，单独执行）:
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128

# ── HuggingFace 生态 ──
transformers==5.14.1
accelerate==1.14.0
peft==0.19.1
huggingface_hub==1.25.1
safetensors==0.8.0

# ── ColPali 检索 ──
colpali-engine==0.3.17

# ── 优化器（需要 >=0.45.3 才支持 Blackwell sm_120）──
bitsandbytes==0.50.0

# ── Flash Attention（RTX 5090 上可选，预编译包可能不含 sm_120 内核）──
# flash-attn 如果装不上，transformers 会自动降级到 SDPA，不影响训练
# flash-attn>=2.7.0

# ── 图像和 HTTP ──
Pillow==12.2.0
requests==2.34.2

# ── 工具 ──
numpy==2.4.4
PyYAML==6.0.3
sentencepiece==0.2.2
einops==0.8.2
tqdm==4.70.0
