# VLM-RAG full dependencies
# Python 3.10+ / Linux / CUDA 12.x / RTX 4090 or higher

# Core
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.45.0
accelerate>=0.30.0
Pillow>=10.0.0
requests>=2.28.0
numpy>=1.24.0
PyYAML>=6.0

# ColPali retrieval
colpali-engine>=0.3.0,<0.4.0
peft>=0.12.0
bitsandbytes>=0.43.0
flash-attn>=2.5.0

# Tokenizer & model utils
sentencepiece>=0.2.0
einops>=0.7.0
tqdm>=4.65.0

# HuggingFace Hub (for model download)
huggingface_hub>=0.20.0
