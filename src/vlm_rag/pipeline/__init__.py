"""Secure end-to-end retrieval, sensitivity gating, and generation.

Imports are intentionally kept lazy because generation provenance helpers are
also used while the package graph is being initialised.
"""
